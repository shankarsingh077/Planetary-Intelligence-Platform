# Entity Resolver Service

Entity Resolver v1 provides normalization, alias handling, and confidence scoring for event entities.

## Current v1 capabilities

1. Name normalization and alias expansion.
2. Canonical entity id derivation.
3. Entity confidence boost for known aliases.
4. Basic hierarchy placeholder fields.
