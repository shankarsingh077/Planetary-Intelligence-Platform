steps:
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - 'gcr.io/$PROJECT_ID/pip-web:latest'
      - '-f'
      - 'Dockerfile.web'
      - '.'
images:
  - 'gcr.io/$PROJECT_ID/pip-web:latest'
timeout: '600s'
